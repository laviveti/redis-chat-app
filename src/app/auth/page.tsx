import { Button } from "@/components/ui/button";
import React from "react";

const page = () => {
  return (
    <div>
      Auth page
      <Button>Button</Button>
    </div>
  );
};

export default page;
